Default and inner-tunnel sites-available config overrides are used to clean up sql and ldap module errors even when the are disabled with "-sql" and "-ldap" in the config files.

loadgen-client is the proto_load Virtual Server configuration.