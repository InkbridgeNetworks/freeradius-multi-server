# load-generator
## Configuration
sites-available/default:
Config overrides are used to clean up sql and ldap module errors even when the are disabled with "-sql" and "-ldap" in the config files.

sites-available/inner-tunnel:
Config overrides are used to clean up sql and ldap module errors even when the are disabled with "-sql" and "-ldap" in the config files.

load-generator: 
The proto_load Virtual Server configuration.